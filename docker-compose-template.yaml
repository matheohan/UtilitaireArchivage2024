version: '3'
services:
  web:
    image: nginx:latest
    ports:
      - "443:443"
    volumes:
      - ./src:/usr/share/nginx/html  
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./certs:/etc/nginx/certs 